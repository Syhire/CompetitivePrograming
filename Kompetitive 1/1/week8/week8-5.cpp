#include <stdio.h>
#include <stack>
#include <queue>
using namespace std;

int main()
{
    int count, countIdx;

    scanf("%d", &count);

    int n, p, q, i;

    stack<bool> seq;

    for (countIdx = 0; countIdx < count; countIdx++)
    {
        scanf("%d %d/%d\n", &n, &seq);
        i = 1;

        while ()

            printf("%d %d\n", n, i);
    }
}
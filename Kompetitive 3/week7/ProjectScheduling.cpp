#include <stdio.h>
#include <vector>
#include <queue>
using namespace std;

int n;
vector<int> edge[110];
// int hasil[110];
queue<int> q;

int topoSort() {
    
}

int topoSortRek()

int main()
{
    while (scanf("%d\n", &n) && n != 0)
    {
        int many, input;
        scanf("%d\n", &many);

        for (int i = 0; i < many; i++)
        {
            scanf("%d\n", &input);
            edge[i].push_back(input);
        }
    }
}
#include <stdio.h>
#include <queue>
using namespace std;

priority_queue<int> a;
priority_queue<int> b;

int main() {
    int loop;
    scanf("%d\n", &loop);

    while (loop--)
    {
        int n, m, k;
        scanf("%d %d %d\n", &n, &m, &k);

        
    }
    
}